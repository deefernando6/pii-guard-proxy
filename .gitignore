# Top-level dev-time deps only — re-installed via `npm install`.
/node_modules/

# Older release artifacts — kept on disk for reference, not in git.
# (1.2.0 lives on its own branch; allow it in.)
dist/pii-guard-proxy_1.2.2*

# Benchmark data — re-downloaded from huggingface.co on demand.
scripts/data/

# Local editor / OS junk
.DS_Store
*.log
*.swp
.vscode/
.idea/
